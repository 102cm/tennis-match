# 테니스 대진 앱 설치 안내

이 앱은 Android와 iOS에서 설치 가능한 PWA입니다. 앱스토어를 거치지 않고 브라우저에서 `홈 화면에 추가`로 설치합니다.

## 1. 배포하기

휴대폰에서 설치하려면 앱 파일을 HTTPS 주소에 올려야 합니다.

가장 쉬운 방법:

1. 이 폴더의 파일 전체를 GitHub 저장소에 업로드합니다.
2. GitHub Pages, Netlify, Vercel 중 하나로 정적 사이트 배포를 켭니다.
3. 배포된 `https://...` 주소를 휴대폰에서 엽니다.

필요한 파일:

- `index.html`
- `styles.css`
- `app.js`
- `manifest.webmanifest`
- `sw.js`
- `seed-data.json`
- `icons/icon-192.png`
- `icons/icon-512.png`

## BTG 회원명단 함께 배포하기

새로 설치하는 운영진도 BTG 클럽과 회원명단을 처음부터 받게 하려면 `seed-data.json`을 함께 배포해야 합니다.

1. BTG 회원명단이 들어있는 기기에서 앱을 엽니다.
2. `BTG` 클럽으로 들어갑니다.
3. 회원명단 영역의 `배포용 데이터` 버튼을 누릅니다.
4. 다운로드된 `seed-data.json` 파일을 GitHub 저장소의 기존 `seed-data.json`과 교체해서 업로드합니다.
5. GitHub Pages 배포가 갱신된 뒤 운영진에게 앱 링크를 공유합니다.

주의: 앱 파일에 들어간 `seed-data.json`은 새로 설치하는 기기의 초기 데이터입니다. 이미 각자 기기에서 수정한 회원명단은 자동으로 덮어쓰지 않고 유지됩니다.

## 2. Android 설치

1. Android 휴대폰에서 Chrome으로 배포 주소를 엽니다.
2. Chrome 메뉴 `⋮`를 누릅니다.
3. `앱 설치` 또는 `홈 화면에 추가`를 선택합니다.
4. 설치 후 홈 화면의 `테니스 대진` 아이콘으로 실행합니다.

## 3. iPhone 설치

1. iPhone에서 Safari로 배포 주소를 엽니다.
2. 하단 공유 버튼을 누릅니다.
3. `홈 화면에 추가`를 선택합니다.
4. `추가`를 누른 뒤 홈 화면의 `테니스 대진` 아이콘으로 실행합니다.

## 4. 오프라인 사용

한 번 접속해서 설치하면 기본 앱 파일은 휴대폰에 캐시됩니다. 회원명단은 각 휴대폰 브라우저 저장소에 저장됩니다.

주의: 회원명단은 서버 계정에 저장되는 방식이 아니라, 설치한 기기 안에 저장됩니다. 다른 휴대폰과 자동 동기화되지는 않습니다.

## 5. 로컬 테스트

PC에서 임시로 확인하려면 이 폴더에서 간단한 서버를 띄운 뒤 브라우저로 열면 됩니다.

```powershell
python -m http.server 8000
```

그 다음 PC 브라우저에서 아래 주소를 엽니다.

```text
http://localhost:8000
```

휴대폰 설치 테스트는 HTTPS 배포 주소에서 진행하는 것이 가장 안정적입니다.
